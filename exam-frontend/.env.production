# 环境标识
VITE_ENV = production
# 后端接口地址（空 = 同源 nginx 代理 /api/）
VITE_API_BASE_URL=
# AI 智能学习伙伴地址（不设 = 同源 nginx 代理 /ai/）
# VITE_AI_BASE_URL is deliberately unset — tutor-api.ts falls back to '/ai'
# 调试开关
VITE_DEBUG = false
